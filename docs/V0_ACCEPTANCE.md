# 烛照 V0 验收清单

> Phase 0-9 完成后的人工回归测试清单。每项验收后打 ✅。

## 一、启动与基础

- [ ] App 启动无白屏（ErrorBoundary 已挂载）
- [ ] 数据库初始化成功（zhuzhao.db，4 条 migration）
- [ ] 主导航 7 个页面可切换
- [ ] Toast 提示正常显示

## 二、Phase 5 Intake（输入流水线）

- [ ] Chat Sidebar 输入"提醒我明天 10 点测试 #phase5test"
- [ ] 触发 LLM Intake，创建 Event + Task + Reminder
- [ ] Dashboard 的"今日输入"显示该 event
- [ ] Tasks 页能看到新任务
- [ ] Reminders 调度器中能看到该 reminder

## 三、Phase 6 任务监督闭环

- [ ] 创建一条 1 分钟后的 reminder
- [ ] 等待 60s，scheduler 自动触发
- [ ] reminder.status 从 pending 变为 fired
- [ ] 关联 task.status 变为 doing
- [ ] Chat Sidebar 出现 assistant 监督追问消息
- [ ] Tasks 页完成任务 → status=done，completed_at 写入
- [ ] Tasks 页延期任务 → delay_count+1
- [ ] 多次延期触发 harsh 文案

## 四、Phase 7 日记与每日总结

- [ ] Journal 页切到某天，显示当天 journal_entries
- [ ] 每条日记显示：raw_content / ai_summary / mood / tags / source_event_id / created_at
- [ ] 点击"生成今日总结"
- [ ] reviews 表写入一条 daily 类型记录
- [ ] 再次生成 → upsert 生效（同日仍 1 条，id 不变）
- [ ] Dashboard 显示今日总结卡片
- [ ] 断开 LLM（无 active provider）→ fallback 模板，App 不崩

## 五、Phase 8 Markdown 导出

- [ ] Settings 设置导出目录
- [ ] 点击"立即导出全部"
- [ ] 生成 5 个文件：Daily / Journal / Tasks / Ideas / Index.md
- [ ] 每个文件顶部有 `Generated by Zhuzhao` 标记
- [ ] Daily 包含 wins / delays / topNext / improvement
- [ ] Journal 保留 raw_content
- [ ] Tasks 包含 status / due_at / delay_count / completed_at
- [ ] Ideas 包含 raw_content
- [ ] 重复导出不会产生重复文件（带标记可覆盖）
- [ ] 手动改一个文件去掉标记 → 再次导出 → 生成 .zhuzhao.md 旁路文件，原文件不动

## 六、Phase 9 安全加固

- [ ] Settings 中尝试把导出目录改为 `C:\Windows` → 拒绝（Rust 端 is_sensitive_path）
- [ ] 尝试路径穿越 `D:\safe\..\..\..\Windows\System32\x.md` → 拒绝
- [ ] "打开导出目录"按钮 → explorer 打开实际目录
- [ ] 把导出目录改为不存在的路径 → "目录不存在" 错误
- [ ] 把导出目录改为文件路径 → "路径不是目录" 错误

## 七、Phase 9 JSON 备份

- [ ] Settings → 数据与安全 → 点击"导出 JSON 备份"
- [ ] 在 Zhuzhao/Backups/ 下生成 `YYYY-MM-DD_HHmm-zhuzhao-backup.json`
- [ ] 备份 JSON 顶部 metadata：generated_by=zhuzhao, exported_at, version, schema_version
- [ ] 备份包含全部业务表（events/tasks/journal/ideas/reviews/...）
- [ ] **关键**：`llm_providers[].api_key` 全部为 null（脱敏）
- [ ] **关键**：app_settings 中无 api_key / secret / token / authorization

## 八、Phase 9 日志与诊断

- [ ] Settings → 诊断与日志区显示最近 300 条日志
- [ ] 按级别过滤（all / error / warn / info）正常
- [ ] 触发一次 LLM 失败 → 日志中出现 llm scope 的 warn 条目
- [ ] 触发一次 reminder 触发 → 日志中出现 reminder scope 的 info 条目
- [ ] 点击"导出日志" → Zhuzhao/Backups/ 下生成 `*-zhuzhao-logs.json`
- [ ] 点击"清空日志" → 第一次点击变为"再次确认清空"
- [ ] 再次点击 → 日志清空，列表为空
- [ ] 日志中无 api_key / authorization / bearer / sk- 等敏感关键词

## 九、Phase 9 错误处理

- [ ] LLM 失败 → toast 提示，App 不崩
- [ ] Markdown 导出失败（路径非法）→ toast 提示具体错误
- [ ] DB 查询失败 → 友好错误，非白屏
- [ ] Reminder scheduler 异常 → 仅日志，App 不崩
- [ ] 渲染层抛错 → ErrorBoundary 接管，显示"重新加载"按钮

## 十、敏感信息扫描

- [ ] Markdown 导出文件全目录扫描：无 api_key / authorization / bearer / sk-
- [ ] JSON 备份文件扫描：无 api_key（应为 null）/ authorization / bearer / sk-
- [ ] 日志 JSON 文件扫描：无 api_key / authorization / bearer / sk-
- [ ] zhuzhao.db 不在 git 仓库中
- [ ] .gitignore 已覆盖 node_modules / dist / target / .trae / logs / .env

## 十一、范围确认（V0 不做）

- [ ] 没有云同步
- [ ] 没有双向 Markdown 同步
- [ ] 没有 Obsidian 插件
- [ ] 没有系统通知
- [ ] 没有外部自动化（飞书 / 邮件 / 微信）
- [ ] 没有移动端
- [ ] 没有账号系统
- [ ] 没有联网遥测
- [ ] 没有自动上传日志
- [ ] 没有自动 git

## 十二、构建验证

```powershell
npx tsc --noEmit     # 0 错误
npm run tauri:dev    # 编译成功，App 启动，无 panic
```

## 验收通过标准

- 一至九全部 ✅
- 十 敏感信息扫描全部 ✅
- 十一 范围确认全部 ✅
- 十二 构建验证通过

## 十三、已知未验收风险（Phase 9 收口记录）

### 1. 非法目录手动拒绝测试暂缓

**状态**：未手测，列为 known risk。

**未手测项**：
- `C:\Windows` 作为 exportDir 保存 / 导出 / 打开目录
- `C:\` 作为 exportDir 保存 / 导出 / 打开目录
- `D:\` 作为 exportDir 保存 / 导出 / 打开目录

**代码层验证已通过**（可查证）：
- Rust 端 `is_sensitive_path` 在 blocked 列表中包含 `windows` / `program files` / `programdata` / `$recycle.bin` / `system volume information` / `boot` / `recovery` 等敏感目录
- Rust 端 `is_path_within` 双重校验，目标路径必须 canonicalize 后仍在 baseDir 之下，否则拒绝
- Rust 端 `validate_export_path` 同时校验：路径存在 + 是目录 + 不在敏感目录 + 在 baseDir 之下
- 5 个旧危险 invoke command（`write_text_file` / `read_text_file` / `list_dir` / `path_exists` / `path_is_dir`）已全部删除，前端无法绕过 baseDir 校验
- `open_directory` 已做 canonicalize + is_sensitive_path 校验

**风险等级**：低
- 代码层安全保证充分
- 正常导出目录测试已通过（exportDir = `D:\2_study\201_AIAgent\1_trae\2_zhuzhao_export`）
- 用户文件保护已验证（52 字节用户文件未被覆盖，旁路 .zhuzhao.md 正确生成）
- JSON 备份 api_key / base_url 脱敏已验证
- 路径穿越代码扫描通过（5 个旧命令零调用）

**待补充手测项**：
- [ ] `C:\Windows` 保存被 toast 拒绝
- [ ] `C:\Windows` 导出被 toast 拒绝
- [ ] `C:\Windows` 打开目录被 toast 拒绝
- [ ] `C:\` 保存 / 导出 / 打开目录被拒绝
- [ ] `D:\` 保存 / 导出 / 打开目录被拒绝
- [ ] 测试完毕后 exportDir 恢复为 `D:\2_study\201_AIAgent\1_trae\2_zhuzhao_export`

### 2. 日志与诊断 UI 交互手测暂缓

**未手测项**：
- info / warn / error 筛选按钮切换效果
- 清空日志二次确认 UI 第一次点击进入确认态
- 导出日志 JSON 文件实际生成

**代码层验证已通过**：
- DiagnosticsSection 组件包含 confirmClear 状态机（false → true → 执行）
- exportLogsJson 调用 write_export_text_file 带安全校验
- log-repo SENSITIVE_PATTERNS 脱敏规则完整（sk- / Bearer / api_key / authorization / secret / token / password）
- 数据库 app_logs 表已存在，当前 3 条 markdown scope 日志，无敏感信息

**风险等级**：低

### 3. Phase 5/6/7 回归手测暂缓

**未手测项**：
- 输入 `日记：Phase 9 回归测试 #phase9regression` 触发 Intake
- 输入 `1分钟后提醒我测试 Phase 9 回归 #phase9reminder` 触发 Reminder
- Journal 页点击"生成今日总结"

**代码层验证已通过**：
- 数据库 events 今日 2 条（user_input 类型，ai_processed=1）✅
- 数据库 journal_entries 今日 1 条（raw_content 完整、tags 正确、source_event_id 关联）✅
- 数据库 reviews 今日 1 条 daily summary（id=01kwr6e8hm02z77gje4z8g2saj，重复生成 upsert 生效）✅
- 数据库 reminders 4 条 pending，等待到点触发 ✅
- 数据库 llm_providers 1 条 active（商汤 deepseek-v4-flash）✅

**风险等级**：低

### 4. ErrorBoundary 触发手测暂缓

**未手测项**：实际抛出渲染错误验证 ErrorBoundary UI 显示

**代码层验证已通过**：
- main.tsx 已用 `<ErrorBoundary><App /></ErrorBoundary>` 包裹
- ErrorBoundary 实现 getDerivedStateFromError / componentDidCatch
- 显示"烛照遇到了问题"卡片 + "重新加载"按钮
- error.message 截断 200 字符，不泄露完整技术信息
- 仅 console.error，不上报，不记录 API Key / Authorization / LLM 原始响应

**风险等级**：极低

